import { WEBPTool } from './webp-tool';

export const metadata = {
  title: "WebP to PNG Converter - imgxLab",
  description: "Effortlessly convert images between WebP and PNG formats, both ways, with ease.",
};

export default function MetadataViewerPage() {
  return (
      <WEBPTool />
  )
}
